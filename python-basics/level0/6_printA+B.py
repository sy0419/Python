a, b = map(int, input().strip().split(' '))
print(f"{a} + {b} = {a + b}")

# f-string allows you to insert variables directly into a string using {}.
# f-string는 파이썬의 문자열 포매팅 방법 중 하나로, 중괄호 {} 안에 변수를 직접 넣을 수 있다.